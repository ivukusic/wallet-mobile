import {
  BackgroundColorProps,
  FlexboxProps,
  LayoutProps,
  PaddingProps,
  SpaceProps,
} from 'styled-system';

export interface Props
  extends SpaceProps,
    LayoutProps,
    FlexboxProps,
    BackgroundColorProps,
    PaddingProps {}
